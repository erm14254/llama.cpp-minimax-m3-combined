# LongCat Sparse Parity — Status 2026-08-17

First authoritative status from the Windows 11 / RTX PRO 6000 workstation.
Supersedes the *indicative* localization in `STATUS_2026-08-16.md`. All
guardrails in `CLAUDE.md`, `NEXT_ACTION.md` and
`HANDOFF_MEMORANDUM_2026-08-15.md` remain in force unchanged.

**Branch:** `claude/longcat-win11` (from `35ad2e63a`)
**Capture:** `cpp_attn0_mla_blackwell_512/` (+ `SHA256SUMS.txt`)

---

## Headline

The Blackwell C++ block-0 MLA capture is **done and gated**, the comparison
against the authoritative HF oracles is **done**, and the first genuine
divergence is not where the Ampere run indicated.

| | Ampere (indicative) | **Blackwell (authoritative)** |
|---|---|---|
| First divergent surface | `kv_a_proj_with_mqa` | `kv_a_proj_with_mqa` |
| First **genuine** divergence | `o_proj` | **`kv_a_layernorm`** |
| Q path | "essentially exact" | **byte-exact, all 3 surfaces** |
| `kv_a_layernorm` | below noise floor, unconcludable | **real, and fully explained** |

**No LongCat arithmetic was changed.** Everything below is measurement.

---

## The capture is authoritative

All three frozen gates reproduce **byte-exact**:

| Artifact | SHA256 | |
|---|---|---|
| `logical0_attn0_resid.bin` | `2c804a35a0397e380d77f08d2a7ffd11fc6e4672722c31a98f7f96620c2a4a4e` | **PASS — the arbiter** |
| `inp_embd_ngram.bin` | `d0e9edc8503e388d62b27f39c6ad24560021b1f4533410bf4f86a28fef4ea45f` | PASS |
| `logical0_attn0_norm.bin` | `a1c4c20cf346cbc5d13a0ea3d1d142f1b54f8e024ff3f04fdb31e9120e75b0af` | PASS |

Placement reproduced the frozen run exactly: `(n_layer, n_part, overflow_type)
= (29, 15, ATTN)`, `id_dense_start=0`, `offloaded 29/30`, `CUDA0 model buffer
88936.14 MiB`, `CPU KV 10.12 MiB` + `CUDA0 KV 283.50 MiB`, compute
`1654.00`/`24.03 MiB`. Tokenization: 512 pieces, all ID 483.

---

## Result

`compare_longcat_attn0_mla_stages.py`, full sequence, 512 tokens:

| Surface | max_abs | rel-RMSE | cosine | exact% | first divergent token |
|---|---|---|---|---|---|
| `q_a_proj` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `q_a_layernorm` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `q_b_proj` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `kv_a_proj_with_mqa` | 0.0254898 | 0.00122158 | 0.9999993376 | 0.00% | 0 |
| `kv_a_layernorm` | 0.0690088 | 0.00162006 | 0.9999999140 | 0.00% | 0 |
| `o_proj` | 0.0279193 | **0.0194126** | 0.9998376748 | 0.00% | 0 |

`analyze_longcat_attn0_mla_bf16_boundary.py` separates precision boundaries
from real differences. Every HF surface is confirmed 100% on the BF16 lattice,
so "already BF16" is verified, not assumed.

| Surface | exact% raw → BF16-rounded | rel-RMSE raw → rounded | verdict |
|---|---|---|---|
| `q_a_proj` / `q_a_layernorm` / `q_b_proj` | 100% → 100% | 0 → 0 | **byte-exact** |
| `kv_a_proj_with_mqa` | 0.00% → **100.00%** | 0.00122 → **0** | **pure BF16 boundary** |
| `kv_a_layernorm` | 0.00% → 45.22% | 0.00162 → 0.000916 | **real** |
| `o_proj` | 0.00% → 12.58% | 0.01941 → 0.01948 | **real** |

### The whole Q path is byte-exact

`q_a_proj`, `q_a_layernorm` and `q_b_proj` are identical to the HF Blackwell
oracles across all 512 tokens — rel-RMSE exactly 0, cosine exactly 1, 100% of
elements. On Ampere these read 2.0e-06 / 0 / 6.2e-06; that residue was the
cross-GPU noise floor, not the model. The Q-BF16 diagnostic is doing its job
exactly, and the memorandum's "Q-BF16 made things worse" is now definitively
reframed: it made Q *correct*.

### `kv_a_proj_with_mqa` is a pure output boundary

Rounding the C++ F32 output to BF16 makes it **exactly** equal to HF — 100.00%
of 512x576 elements, rel-RMSE 0. Not 99.31% as Ampere indicated; **perfect**.
C++ computes the GEMM in F32 where HF stores BF16; nothing else differs.

### `kv_a_layernorm` is the first genuine divergence — and it is explained

This is the reading that sat below the Ampere noise floor and genuinely
required this run. `analyze_longcat_attn0_kv_a_norm_semantics.py` reconstructs
the norm offline from the GGUF weight the C++ run actually loaded:

1. **Sanity** — the offline F32 model reproduces the **C++** surface at
   rel-RMSE 0, 100.00% exact. The reconstruction and weight are correct.
2. **Control** — `bf16(C++ kv_a_proj[:, :512]) == HF kv_a_proj[:, :512]` is
   `True`. The upstream input differs *only* by the rounding boundary above.
3. **Sweep** — feeding HF's own byte-identical BF16 input through llama.cpp's
   F32 norm still does **not** reproduce HF (rel-RMSE 0.00208, 0% exact). So
   the divergence is in the norm's precision semantics, not its input.

| Cast ordering (eps=1e-6) | rel-RMSE | exact% |
|---|---|---|
| A `f32(norm) * f32(w)` → f32 — **llama.cpp today** | 0.00207546 | 0.00% |
| B `bf16( f32(norm) * f32(w) )` | 0.000359351 | 68.59% |
| **C `bf16( bf16(norm) * f32(w) )`** | **0** | **100.00%** |
| **D `bf16( bf16(norm) * bf16(w) )`** — HF RMSNorm pattern | **0** | **100.00%** |
| E `bf16(norm) * bf16(w)` → f32 | 0.0010619 | 2.54% |

**HF rounds the normalized activation to BF16 *before* the weight multiply,
and rounds the product to BF16. llama.cpp keeps both in F32.** That single
ordering difference accounts for the surface *exactly* — max_abs 0.

C and D coincide because the norm weight is already 100% on the BF16 lattice.
eps 1e-6 and 1e-8 both reproduce exactly; **1e-5 is excluded** (rel-RMSE
9.5e-07), independently confirming the 1e-6 the C++ already uses.

### `o_proj` — real, but its input is already wrong

rel-RMSE 0.0194, ~12x `kv_a_layernorm`, and BF16 rounding does not touch it
(0.01941 → 0.01948). It is a genuine divergence. **But no independent defect is
claimed**: `o_proj` consumes K/V derived from `kv_a_layernorm`, which is itself
divergent, so its error is at least partly inherited. Whether anything remains
wrong *after* the KV boundaries are corrected cannot be determined from this
capture and must be re-measured, not assumed.

This corrects the Ampere reading, which named `o_proj` the first genuine
divergence only because `kv_a_layernorm` sat below its noise floor.

---

## Environment finding — cuBLAS version silently breaks every gate

The first gated run **failed**: `logical0_attn0_resid` came out
`49d729e16b09b7d113e1d774851364b4c31ee1ff98608505cbe52122824fc928` while both
upstream anchors passed and placement was bit-identical to the frozen run.

Running the **historical** `llama-debug.exe` — the exact binary that produced
`2c804a35…` on 2026-08-15 — reproduced the same `49d729e1…`. So the rebuild was
never at fault; the frozen residual had stopped being reproducible on this
machine.

Cause: `ggml-cuda.dll` imports **`cublas64_13.dll` by bare name**, and this
machine's `PATH` lists `CUDA\v13.0\bin\x64` **before** `v13.2`. The process was
loading cuBLAS **6.14.11.1300** (v13.0) instead of the **6.14.11.1330** (v13.2)
the build targets. Prepending the v13.2 directory restored `2c804a35…`
byte-exact.

The failure signature is diagnostic in itself: custom elementwise kernels
(`inp_embd_ngram`, `attn_norm-0`) stayed byte-exact while the GEMM-dependent
residual moved — exactly what a cuBLAS kernel-selection change produces.

> **Every future run on this workstation must pin cuBLAS to v13.2**, e.g. by
> prepending `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.2\bin\x64`
> to `PATH` for the child process. Without it the frozen gates fail silently
> and any comparison is invalid.

---

## Corrections to `WIN11_CPP_MLA_CAPTURE_RUNBOOK.md`

Its section 3 invocation does not reproduce the baseline. Evidence from the
frozen log `cpp_attn0_hf_rmsnorm_mlaeps1e6_qbf16_512.log` and the source:

| Runbook says | Reality |
|---|---|
| `-ngl 99` | **not passed.** `common/fit.cpp:378` throws `"n_gpu_layers already set by user … abort"`, yet the frozen logs all end `successfully fit params to free device memory` |
| `-ot ffn_*=CUDA_Host` | **not passed.** No override line appears anywhere; the MoE split came from auto-fit's `n_part` mechanism |
| "offloaded 30/30 layers" | **29/30.** The 30/30 is the fit's *initial probe*; every final load is 29/30 |
| (not mentioned) | **`-fitt 4096` required.** Default is 1024 MiB (`common/common.h:473`); the frozen log records `free memory target of 4096 MiB` |
| (not mentioned) | **`-b 4608` required** to match `n_batch` (verified numerically inert, but it aligns the log) |
| (not mentioned) | **`-v` required.** `LOG_TRC` needs verbosity ≥ 4 and `LOG_DBG` ≥ 5; default is 3 (`common/log.cpp:29`), so without it the fit trace is not emitted at all |
| (not mentioned) | **cuBLAS must be pinned to v13.2** (above) |

Also worth recording: `common/log.cpp:89-93` sends `GGML_LOG_LEVEL_NONE` to
**stdout** and everything else to **stderr**. The token-piece output is `LOG(...)`
at level `NONE`, so the tokenization gate reads stdout while the placement gate
reads stderr. Redirecting the two streams to separate files splits the gates.

`-ctk f32 -ctv f32` is confirmed by arithmetic, not just by the runbook:
4608 x 576 x **4 B** = 10.125 MiB/layer, matching `CPU KV 10.12 MiB` (1 layer)
+ `CUDA0 KV 283.50 MiB` (28 layers) = 29 layers.

---

## Build provenance

Fresh build directory `D:\llama.cpp-longcat-claude-build-cuda132`, configured
with venv cmake 4.4.2, VS 17 2022 x64, CUDA v13.2 toolset, `GGML_CUDA=ON`,
`CMAKE_CUDA_ARCHITECTURES=120a-real`, `LLAMA_BUILD_TESTS=OFF`.

- **182/182** reproducibility-relevant `CMakeCache` entries identical to the
  historical build. The 17 differences are all generated or path-dependent and
  were recorded, not forced.
- Compile-relevant sources vs `pre-gate4`: `ggml/` (1272 files) and
  `examples/debug/` (3) byte-identical in full; across `src/` (209) and
  `common/` (71) exactly two files differ — the known callback-only instrumented
  pair. Every CUDA kernel is bit-for-bit the frozen source.
- The historical build passes an extra `-I …\v13.2\include\cccl`; investigated
  and proven **inert** — nvcc reaches the same directory through a built-in
  path, 1374/1377 headers resolve to the same file and the other 3 are
  byte-identical duplicates.
- `llama-debug.exe`, `llama.dll`, `ggml.dll`, `ggml-cpu.dll`, `ggml-base.dll`
  match historical sizes exactly. `llama-common.dll` +5,120 B (instrumented
  `debug.cpp`). `ggml-cuda.dll` +13,312 B — the frozen gate settles equivalence
  empirically, and the historical binary was shown to produce identical output
  to the rebuild.

---

## Next action

The first genuine divergence is identified **and** its mechanism is known, so
the standing block on changing arithmetic is discharged for this specific
boundary. The narrowest defensible next step:

1. Add the missing BF16 boundary at `kv_a_proj_with_mqa` (output → BF16).
2. Apply HF RMSNorm cast semantics at `kv_a_layernorm`:
   `bf16( bf16(x * rsqrt(var + eps)) * w )`.
3. Re-run this capture and re-measure. Both KV surfaces should become
   byte-exact; then re-measure `o_proj` to see what, if anything, of its
   0.0194 survives once its input is correct.

Still forbidden until that re-measurement lands: touching MLP/MoE, a production
FA fix, a repaired 2050 run, and any widening of the frozen logit criterion.

---

## Artifacts

| Path | Contents |
|---|---|
| `cpp_attn0_mla_blackwell_512/` | six MLA surfaces + sidecars, frozen anchors, `SHA256SUMS.txt` |
| `…/comparison_authoritative.json` | full-sequence HF↔C++ metrics |
| `…/bf16_boundary_authoritative.json` | boundary-vs-real classification |
| `…/kv_a_norm_semantics_authoritative.json` | cast-ordering sweep |
| `analyze_longcat_attn0_mla_bf16_boundary.py` | `67d6dae7dc78670441bf64d804d966b6cee6013513c8d29191018cd5bf59459e` |
| `analyze_longcat_attn0_kv_a_norm_semantics.py` | see `ARTIFACT_SHA256_20260817.txt` |

Both new scripts are `python -m py_compile` clean. `*.bin` and `*.log` remain
gitignored; the run logs stay on disk beside the capture.

---

# Addendum 2026-08-17 — Experiment A result (KV BF16 boundary + norm cast semantics)

Executed after independent adversarial review (Fable 5 Max) **CONFIRMED WITH
CORRECTIONS**: experiments staged as A (the two oracle-gated changes) then B
(post-scale round, ungated, separate); cuBLAS contract made durable in
`CLAUDE.md`/`NEXT_ACTION.md` first; `2c804a35…` retired as a gate and preserved
as the immutable old-arithmetic baseline. The review also corrected the epsilon
claim — eps=1e-6 is fixed **by source** (`LongcatFlashRMSNorm.__init__` default,
constructed without override); the sweep excludes 1e-5 by one element and
cannot distinguish 1e-6 from 1e-8 — and verified the projection boundary at
integer precision: 294,912/294,912 elements, both splits.

**Commit `3d16341dc`** — `il == 0` KV branch only, mirror of the accepted
Q-side pattern: full-576 BF16 roundtrip on `kv_cmpr_pe` before the split views
(D1), and `rms_norm(1e-6)` → BF16 → F32 → `ggml_mul(w)` → BF16 with the
callback on the rounded tensor (D2). No post-scale round.

**Run** `cpp_attn0_mla_expA_512/`: preflight verified placement
`(29, 15, ATTN), id_dense_start=0` **and the live-process cuBLAS module**
(`v13.2\bin\x64\cublas64_13.dll`, 6.14.11.1330). Tokenization 512 × `(483)`.

## Gates — all PASS

| Surface | Result |
|---|---|
| `inp_embd_ngram` / `logical0_attn0_norm` | `d0e9edc8…` / `a1c4c20c…` unchanged |
| Q trio | `ddf69fe4…` / `956bd3e8…` / `4f3b647b…` unchanged |
| **`kv_a_proj_with_mqa`** | **= HF oracle `513390…` byte-exact** |
| **`kv_a_layernorm`** | **= HF oracle `b44cc101…` byte-exact** |

The in-graph run reproduced the offline byte-exact predictions exactly.
**Five of the six MLA surfaces are now hash-identical to the HF oracles.**

## Measurements (not gates)

| Quantity | Old arithmetic | Experiment A |
|---|---|---|
| `o_proj` rel-RMSE | 0.0194126 | **0.00606403** (÷3.2) |
| `o_proj` after BF16 rounding | 0.0196 (12.54% exact) | 0.006276 (26.61% exact) — still REAL |
| attn0 residual vs HF `2e9b65eb…` | rel-RMSE 0.0152371 | **0.00568733** (÷2.7), max_abs 0.0032950 |
| residual hash | `2c804a35…` (retired baseline) | `ce1b7836…` (recorded) |

Reading: the majority of the historical `o_proj`/residual divergence was
inherited from the KV path, as hypothesized. The surviving 0.006 remainder is
real and lies in the downstream cluster: the post-scale BF16 round (HF
`modeling_longcat_flash.py:426` — **Experiment B**), then bf16 cos/sin RoPE
(`:124`, `:433`), KV-cache dtype semantics, softmax `.to(query.dtype)`
(`:271`), absorption order, and the `wo` GEMM.

Artifacts: `cpp_attn0_mla_expA_512/` with `SHA256SUMS.txt` (33 files),
`comparison_expA.json`, `bf16_boundary_expA.json`. Source
`src/models/longcat-flash-ngram.cpp` = `21d62d48…` after commit `3d16341dc`.

---

# Addendum 2026-08-17 — Experiment B result (post-scale BF16 round)

**Commit `5b408206c`** — the one remaining source-documented KV boundary,
separated from Experiment A per review because no capture surface gates it:
HF scales the BF16 `k_pass` tensor so the scaled result is stored in BF16
(`modeling_longcat_flash.py:426`); llama.cpp scaled in F32. For `il == 0`:
scale in F32 → round BF16 → widen F32, mirroring the accepted Q-side
post-scale rounding. Four non-comment lines.

**Run** `cpp_attn0_mla_expB_512/`: preflight verified placement and the
live-process cuBLAS module (v13.2, 6.14.11.1330), as in A.

## Gates — all Experiment A gates still PASS

Anchors, Q trio, and both KV surfaces unchanged and byte-exact to the HF
oracles (every gated surface is upstream of the scale). 7/7 PASS.

## Attribution — delta(A→B)

| Quantity | Old arithmetic | Exp A | **Exp B** |
|---|---|---|---|
| `o_proj` rel-RMSE (vs HF oracle) | 0.0194126 | 0.00606403 | **0.00475900** |
| `o_proj` exact after BF16 round | 12.54% | 26.61% | 33.25% — still REAL |
| attn0 residual rel-RMSE (vs HF `2e9b65eb…`) | 0.0152371 | 0.00568733 | **0.00390108** |
| residual max_abs | 0.0135617 | 0.0032950 | **0.0010450** |
| residual hash | `2c804a35…` | `ce1b7836…` | `7e05940b…` |

The post-scale round moves both downstream measurements toward HF by a further
~22-31% on top of A. Cumulative from the old arithmetic: residual **3.9×**
closer, `o_proj` **4.1×** closer. The surviving `o_proj` remainder
(rel-RMSE 0.00476; BF16 rounding does not explain it — 0.005004 after,
33.25% exact) is the genuine downstream divergence, now attributable to the
still-unmodeled cluster in HF source order: bf16 cos/sin RoPE (`:124`,
`:433`), bf16 attention path (softmax `.to(query.dtype)` `:271`, bf16
SDPA/cache vs C++ F32 cache), the absorption ordering (llama.cpp folds `wk_b`
into `q_nope` where HF does not), and the `wo` GEMM input dtype.

Artifacts: `cpp_attn0_mla_expB_512/` with `SHA256SUMS.txt` (33 files),
`comparison_expB.json`, `bf16_boundary_expB.json`. Source
`src/models/longcat-flash-ngram.cpp` = `42bd0db2…` after commit `5b408206c`.

## Standing state after A + B

Six of six pre-attention block-0 MLA surfaces (Q trio, both KV surfaces, plus
the `kv_cmpr_pe` full-576 projection) are byte-exact to the HF Blackwell
oracles. The first genuine divergence is now **inside the attention/output
path** (`o_proj`), exactly where the memorandum's interpretation table pointed
next. Still forbidden until that remainder is diagnosed: MLP/MoE work, a
production FA patch, a repaired 2050 run, and any widening of the frozen logit
criterion.

---

# Addendum 2026-08-17 — Attention-path localization (measurement-only): first divergence is Q/K RoPE

Executed per the approved S1→S2→S2b→S3→S4 design. **No arithmetic changed**
(commit `5dda40c50` is dump/hook plumbing only, proven inert below).

## Instrumentation validity — both regression classes pass

**C++ run** `cpp_attn0_mla_attnpath_512/` (pinned + live-module-verified
cuBLAS 6.14.11.1330, placement `(29, 15, ATTN)`):

- Class 1 — HF-exact gated surfaces keep their oracle hashes: anchors + Q trio
  + both KV surfaces, 7/7 PASS.
- Class 2 — every pre-existing non-HF-exact output byte-identical to
  Experiment B: `o_proj`, `logical0_attn0_resid`, `mlp0`/`attn1` resid,
  `result_norm`, all 13 `logical_*`, 18/18 PASS.

**HF run** `D:\lc_mla_blackwell_attn\` (extended
`capture_longcat_hf_attn0_mla_stages.py`, SHA `0b89a787…`): gates 1-3 PASS
byte-exact, and all six original surfaces re-emitted **hash-identical** to
`D:\lc_mla_blackwell`. New oracles: `rope_cos` `8771da1e…`, `rope_sin`
`5c5dede9…`, `attn_o_input` `965760c8…`; `summary.json` records
`attn_scaling = 0.15781141572701202` (= mscale²/√192, mscale = 1.4787…),
`mla_scale_q_lora = √2`, `mla_scale_kv_lora = √6`. Notably HF folds yarn
mscale into the softmax scaling — the captured cos/sin are unscaled (max 1.0)
and **BF16**.

## Segment results (`analyze_longcat_attn_path_localization.py`, SHA `8c481349…`)

| Segment | raw equal | bf16-rounded equal | rel-RMSE | verdict |
|---|---|---|---|---|
| **S1 `q_pe_rope`** | 6,703/1,048,576 | 849,362/1,048,576 | 0.000625 | **REAL — first divergence** |
| S2 `k_pe_rope` | 138/32,768 | 26,553/32,768 | 0.000858 | REAL (same mechanism) |
| S2b `kv_cmpr_scaled` | **262,144/262,144** | 262,144/262,144 | **0** | **byte-exact** |
| S3 `kqv_out` vs `attn_o_input` | 42/2,097,152 | 1,061,027/2,097,152 | 0.00387 | REAL, but **inherits S1/S2** |
| S4a analytic `wo` on oracle ctx | **1,572,864/1,572,864** | — | **0** | byte-exact (analytic only) |
| S4b `bf16(kqv_out)` vs ctx oracle | — | 50.6% | 0.00396 | the `wo` input error |
| S4c real ggml `wo` on C++ ctx | 12/1,572,864 | 33.2% | 0.004759 | = known `o_proj` result |

**Comparison-harness validation:** at token 0 (cos=1, sin=0 — rope is
identity) the C++ dumps equal the mapped inputs **2048/2048** (Q) and
**64/64** (K), proving the permutation P, per-head slicing, and scale
semantics of the comparison are exactly right. The reference is reproduced
identically by torch-bf16-CUDA and a stepwise numpy RNE model.

## Localization verdict

1. **The first genuine divergence in the attention path is RoPE (H1):** HF
   computes rotary with **BF16 cos/sin** (`modeling_longcat_flash.py:124`) and
   BF16 elementwise arithmetic (`:322-331`), while `ggml_rope_ext` computes
   F32 cos/sin and F32 arithmetic. Divergence appears exactly where rotation
   is nontrivial (t=0 exact; error profile at bf16-ulp relative scale, which
   on Q values reaching ~2.7e3 explains max_abs 3.69). S2 shows the identical
   mechanism on the 64-wide rotary-K cache component.
2. **H2's value question is fully closed:** the 512-wide post-scale
   compressed-KV cache input is **byte-exact** (S2b), and the rotary-K half
   differs only via H1.
3. **H3/H4 attribution is NOT licensed:** S3 is REAL but its inputs already
   carry the S1/S2 rope divergence, so per the approved stop rule the
   orthogonal-variant attribution does not proceed. H3/H4 remain bracketed
   inside S3, to be re-measured once RoPE parity is established.
4. **H5 semantics characterized (analytic only):** a plain torch-bf16 GEMM of
   the oracle context against the GGUF `wo` reproduces the `o_proj` oracle
   **byte-exact** (max_abs 0) — the `wo` boundary is an ordinary
   bf16-in/bf16-out linear with f32 accumulation. This is not a test of ggml's
   `wo` execution; that test only becomes meaningful with a clean context.

Artifacts: `cpp_attn0_mla_attnpath_512/` (40-entry `SHA256SUMS.txt`,
`attn_path_localization.json`), `D:\lc_mla_blackwell_attn\` (19-entry
manifest; sidecars mirrored to
`_external_artifacts/hf_mla_blackwell_attn_20260817/`).

**Next action (not begun):** design a block-0 RoPE precision experiment — HF
BF16 cos/sin + BF16 elementwise semantics for `q_pe`/`k_pe` at `il == 0` —
gated byte-exact on reproducing the S1/S2 references, then re-measure S3 with
clean inputs to unlock H3/H4. Still forbidden: MLP/MoE, production FA, 2050
runs, widening the frozen logit criterion.

---

# Addendum 2026-08-17 — Experiment R0: RoPE oracle-injection control

**R0 is an oracle-injection control only.** It is not a RoPE implementation,
not an arithmetic fix, and not evidence that C++ reproduces HF rotary
arithmetic. Commit `9a7de8a76` adds env-gated callback injection
(`common/debug.cpp` only): with `LONGCAT_ROPE_INJECT_DIR` set, the post-RoPE
block-0 tensors `q_pe-0`/`k_pe-0` (disambiguated from the pre-RoPE views by
`op == GGML_OP_ROPE`) are overwritten with canonical HF targets immediately
after the node computes.

**Canonical targets** (`make_longcat_rope_targets.py`, SHA `4e737d8c…`):
produced by executing the *actual installed*
`apply_rotary_pos_emb_interleave` on SHA-verified captured oracles, then
canonicalized through proven permutation P into the C++ interleaved layout.
Pre-registered and reproduced exactly:
`q_pe_rope_target` = `c8b9b6bf…` (4,194,304 B), `k_pe_rope_target` =
`3ed6f4e7…` (131,072 B). The unique exact cast ordering is C2 — BF16
store-round after every binary op — 0/1,048,576 (Q) and 0/32,768 (K)
mismatches vs the HF function; single-final-round misses by 162,146,
no-final-round by 872,330. Scope wording: ggml's *existing F32 trig results*
provably cannot round to all captured BF16 cos/sin (sin 3,377/16,384 certain
mismatches, cos 34, by 2×2 solve); this does **not** establish that
HF-equivalent angle generation is impossible in principle.

**Run** `cpp_attn0_mla_r0_512/` (pinned cuBLAS 6.14.11.1330 live-verified,
`(29, 15, ATTN)`, `graphs reused = 0`, both `LONGCAT_ROPE_INJECT` log lines
present): **all gates pass** — both canonical rope gates byte-exact through
the dependent dumps (proving the injection landed and propagated), all eight
upstream regression surfaces unchanged, and `kqv_out` moved off `fce2bb98…`.

## The causal answer

| Measurement | Real C++ rope (attnpath) | **R0 (exact HF rope injected)** |
|---|---|---|
| S3 `kqv_out` vs ctx oracle | 0.00386866 (50.60% bf16-exact) | **0.00384568** (50.67%) |
| `o_proj` vs oracle | 0.00475900 (33.25%) | **0.00473839** (33.31%) |
| residual vs HF `2e9b65eb…` | 0.00390108 | **0.00393128** |

**With exact HF RoPE outputs supplied to the existing downstream C++ graph,
the surviving divergence is essentially unchanged.** The S1/S2 RoPE
divergence, though genuinely the first in the execution order, contributes
almost nothing to the downstream error. The dominant remaining divergence
lives **inside the attention core** — the H3/H4/cache-compute cluster (bf16
expand-first SDPA vs f32 latent-absorbed attention) — which stays bracketed
in S3 per the standing stop rule. No H3/H4 attribution is made here.

Artifacts: `cpp_attn0_mla_r0_512/` (39-entry `SHA256SUMS.txt`),
`rope_targets/` sidecars + manifest (bins gitignored, regenerable
byte-exactly).

---

# Addendum 2026-08-17 — Experiment R1: real in-graph BF16 rotation PASSES both RoPE gates and agrees with R0

**Commit `1cba1760b`** — `il == 0` only, active only under
`LONGCAT_ROPE_ORACLE_DIR`: a file-local diagnostic graph input (inside the
model file — no llama-graph, ggml, RoPE-kernel, or production changes) feeds
the captured Blackwell `rope_cos`/`rope_sin` oracles ([32, 1, 512] first-half
columns, SHA-verified by harness preflight, 512-token `pos[i]==i` asserted),
and the rotation is built from existing ops with the source-proven **C2**
semantics: BF16 inputs, BF16 cos/sin, **BF16 store-round after every binary
op** (cont/cast, pair-reshape [64]→[2,32], strided even/odd views, four
`ggml_mul` + `ggml_sub` + `ggml_add` in BF16, pair-concat re-interleave,
lossless F32 widen). `il > 0` and unset-env take the standard `ggml_rope_ext`
path unchanged.

**Run** `cpp_attn0_mla_r1_512/` (pinned cuBLAS 6.14.11.1330 live-verified,
`(29, 15, ATTN)`, injection OFF):

- **Both canonical RoPE gates PASS with real ggml graph arithmetic:**
  `q_pe_rope.bin` = `c8b9b6bf…`, `k_pe_rope.bin` = `3ed6f4e7…` byte-exact.
- All eight upstream regression surfaces unchanged.
- **R1 downstream agrees with R0 on every surface** — `kqv_out`, `o_proj`,
  `logical0_attn0_resid`, `mlp0`/`attn1` resid, `result_norm` all
  hash-identical between the two experiments.

## Decision-tree outcome (best branch)

Real ggml/CUDA graph arithmetic **reproduces HF's rotary computation exactly
when the angle values are exact** — ggml's BF16 elementwise ops implement the
C2 cast semantics bit-for-bit. The agreement of R1 (real arithmetic) with R0
(callback injection) on all six downstream hashes also cross-validates both
mechanisms: two entirely different routes to the same exact RoPE values
produce identical graphs downstream.

**The remaining production problem for RoPE is angle/cos-sin generation
only** — reproducing HF's yarn-ramped f32 angles (then BF16-rounded) without
consuming captured data. Per the pre-registered scope wording: ggml's
*existing* F32 trig provably cannot round to all captured BF16 values (sin
3,377/16,384), but HF-equivalent angle generation is not established as
impossible in principle — that design is deliberately **not begun** here.

## Standing conclusion after R0 + R1

1. RoPE (S1/S2) is byte-exactly solved in-graph given exact angles (R1), and
   its downstream contribution is near-nil (R0): S3 0.003869 → 0.003846,
   `o_proj` 0.004759 → 0.004738, residual ≈ unchanged.
2. **The dominant surviving divergence is the attention core** — the
   H3/H4/cache-compute cluster (bf16 expand-first SDPA vs f32 latent-absorbed
   attention), bracketed in S3 with clean inputs now available from either
   R0 or R1 state.
3. Stopped here per plan: no production angle-generation design, no H3/H4
   attribution or arithmetic, no MLP/MoE, no production FA, no 2050 runs, no
   widening of any frozen criterion.

Artifacts: `cpp_attn0_mla_r1_512/` (39-entry `SHA256SUMS.txt`). Source
`src/models/longcat-flash-ngram.cpp` = post-`1cba1760b`.

---

# Addendum 2026-08-17 — Attention-core attribution (offline orthogonal grid)

`analyze_longcat_attn_core_attribution.py` (SHA `0453107d…`), fully offline —
no captures, no C++ edits. Contract enforced and recorded: PyTorch-2.13
`torch.backends.cuda.matmul.fp32_precision = "ieee"` (asserted before every
reference GEMM; legacy toggles untouched; `TORCH_ALLOW_TF32_CUBLAS_OVERRIDE`
absent), TF32 RNE quantizer (`0x0FFF + tie-lsb`) self-test PASS incl.
ties-to-even both parities and non-finite rejection.

**P0 weight provenance: PASS.** GGUF `blk.0.attn_k_b.weight` equals the HF
`kv_b_proj` k-rows under a per-head transpose and `attn_v_b` equals the
v-rows directly — **2,097,152/2,097,152 each**, so the HF weight is validly
shared by both variant families.

## Grid results (vs context oracle `965760c8…`; MEAS = R0 `kqv_out`)

| Variant | rel-RMSE vs oracle | vs MEAS | Reading |
|---|---|---|---|
| V1 fused efficient SDPA | **0** (byte-exact) | — | anchor |
| V8 decomposed bf16 | 0.005508 | — | V1↔V8 decomposition offset is **large** (0.0055): naive bf16-everywhere decomposition ≠ fused kernel |
| V2 = V8, probs stay f32 | 0.005440 | — | **H3 softmax-cast: EXCLUDED** (V8↔V2 ≈ 1% effect) |
| V3 decomposed true f32 | 0.002499 | — | precision bundle (V8↔V3): bf16 decomposition *hurts* vs f32 — the fused kernel behaves closer to f32-with-bf16-inputs |
| V4 = V3 + TF32-RNE operands | 0.002492 | — | V3↔V4 ≈ 0: **bf16 ⊂ TF32 lattice**, so TF32 *operand quantization* is lossless on lattice inputs; only non-lattice intermediates (probs, GEMM outputs) can be affected |
| V5 latent-absorbed true f32 | 0.002499 | 0.002956 | **H4 ordering: EXCLUDED** — direct V3↔V5 delta = **rel 1.09e-6** (3,500× below the signal) |
| V6 C++-faithful dtypes | 0.003954 | 0.001995 | dtype discipline moves toward MEAS |
| V7 = V6 + TF32 on F32 GEMMs | 0.003951 | **0.001977** | best MEAS model, but see verdict |

Measured reference reduction discrepancy (f64-accum cross-check of the same
TF32-quantized operands): **rel 2.98e-4**. V9 not needed (V2/V8 unambiguous).

## V7↔MEAS verdict — per the pre-agreed stop rule

V7 sits at rel **0.00198 = 6.6× the measured reference reduction
discrepancy** (the scripted 10× leniency flag technically passed, but 6.6×
with eq=65/2,097,152 is **not a collapse**), and the dispatch-derived
prediction that `kqv_out` lies on the BF16 lattice was **falsified**
(108/2,097,152) — concrete proof that at least the BF16-dst assumption for
the BF16-weight GEMMs was misread. Conclusion, verbatim per plan: *"The
simplified offline model does not fully reproduce the measured C++
execution; the missing detail may include cuBLAS TF32
algorithm/tiling/reduction behavior or another unmodeled operation in the
C++ path."* No new high-level attention mechanism is inferred from the
emulator's failure.

## What the orthogonal pairs did establish

1. **H4 (expand-first vs latent-absorbed ordering) is excluded** — 1.09e-6.
2. **H3 (softmax probs-cast) is excluded** — ~1% of either endpoint's error.
3. **Scale was already excluded** (identical to 1 ulp).
4. Both orderings at exact f32 sit **0.0025** from the bf16-fused oracle —
   a shared floor from comparing any decomposed f32 pipeline against the
   fused bf16 kernel; the **C++-specific excess** (MEAS at 0.0038 vs oracle;
   0.0030 vs its own ideal-f32 model) lives in **real-kernel execution
   detail** (TF32 `GemmBatchedEx` internals and/or the exact GEMM
   path/rounding actually taken), not in ordering, softmax semantics, or
   the algebra.

Stopped here per plan. Candidate next steps (user decision, not begun):
pin the actual mul_mat branch/rounding the C++ run takes for the BF16-weight
and F32 GEMMs (e.g., a one-off instrumented probe or GGML env toggles such as
`GGML_CUDA_CUBLAS_COMPUTE_TYPE`), or accept the attention-core residual as
kernel-level and re-rank priorities (production RoPE angle generation,
`wo`/residual chain) against the frozen logit criterion.

---

# Addendum 2026-08-17 — Attention-core mechanism CLOSED (corrected dispatch + V7″ collapse)

The V7 falsification is fully explained by a mis-read ggml dispatch detail,
and the corrected offline model **collapses onto the measured C++ endpoint**.

## The dispatch, re-derived (static)

At ne11 = 512 all four S3-bracket GEMMs reach `ggml_cuda_mul_mat_cublas`
(mmvf/mmf/mmvq/mmq excluded), `compute_type = src0->type`
([ggml-cuda.cu:1620]; `GGML_PREC_F32` reinforces F32 for the score GEMM).
**The previously-misread detail:** for BF16 compute on NVIDIA,
`prefer_f32_output == true` ([:1510-1519]) → the BF16-weight GEMMs (absorb,
`wv_b`) run `cublasGemmBatchedEx` with A/B = `CUDA_R_16BF`,
**C = `CUDA_R_32F`**, `CUBLAS_COMPUTE_32F` — **F32 output, no BF16 rounding,
no temporary** — exactly why `kqv_out` is off the BF16 lattice
(108/2,097,152). The F32×F32 GEMMs (score, latent context) run batched under
the handle-global `CUBLAS_TF32_TENSOR_OP_MATH` ([common.cuh:1502]).
`cublasSgemm` is only the unbatched special case; none of the four hits it.

## V7″ — corrected model, collapse

`analyze_longcat_attn_core_mechanism.py` (SHA `fbb39b9e…`; IEEE contract,
TF32-RNE self-test, fail-closed override check, SHA-gated inputs):

| Comparison | rel-RMSE |
|---|---|
| **V7″ ↔ MEAS** | **2.344e-4 = 0.98× the measured reference reduction discrepancy (2.400e-4) — collapse** |
| V7″ ↔ ORACLE | 0.0038462 (= MEAS ↔ ORACLE 0.0038457) |
| no-TF32 control ↔ MEAS | 7.15e-4 (3× worse — TF32 on the off-lattice operands is a required component) |

**Mechanism statement (complete):** the C++ attention-core divergence from HF
is the composition of (i) **F32 GEMM outputs staying unrounded** where HF is
bf16 (absorb output, `kqv_out`); (ii) **TF32 quantization of the two
off-lattice F32 operands** — `q_abs` in the score GEMM and probs in the
context GEMM; (iii) the **lossy src1→BF16 conversion of the latent context
entering `wv_b`**. Ordering, softmax semantics, scale, and weight provenance
were all previously excluded. Nothing else is required to reproduce the
measured endpoint to reduction noise.

`GGML_CUDA_CUBLAS_COMPUTE_TYPE` (:1630-1659) is recorded for provenance: it
overrides the compute type globally (all layers, all mul_mats on the path)
and offers no F32-without-TF32 value, so it cannot isolate TF32; with the
mechanism closed, no compute-type A/B run is needed.

Scope of proof: static ggml dispatch + the corrected V7″ model reproduce MEAS
to the measured reference discrepancy; a zero-code-change runtime cuBLAS log
(next commit) directly confirms the API-level dispatch/type semantics.
Internal cuBLAS algorithm/tiling is not independently identified unless the
log exposes it — and **no additional algorithm/tiling mechanism is required
to explain the endpoint at the current measurement resolution.**

## Runtime confirmation (zero code changes) — API-level dispatch directly observed

Probe run `cpp_attn0_mla_probe_512/`: the exact frozen R1-state invocation
with `CUBLAS_LOGINFO_DBG=1`/`CUBLAS_LOGDEST_DBG` (cuBLASLt log opportunistic).
**Inertness gate: all 39 R1 SHAs byte-identical** — the logging perturbed
nothing. `graphs reused = 0`; pinned cuBLAS live-verified (DLL 6.14.11.1330;
the library self-reports cuBLAS v13.3.0, the pairing shipped in CUDA 13.2).

**Block-0 association protocol: all assertions PASS** — sandwich
453 < 492 < 528 < 564 < 1236 (first score/context/`wv_b` strictly between
first and second absorb), zero same-signature entries before first(absorb),
the 15 earlier GEMMs are non-attention shapes (n-gram projections
3072×512×256), `n_kv = 512` read from the run, occurrence counts 28 per
signature (recorded, not assumed; one layer resides on CPU per the frozen
placement and CPU GEMMs do not appear in the cuBLAS log).

| Block-0 call | fn (observed) | m,n,k,batch | A/B → C | compute | algo |
|---|---|---|---|---|---|
| absorb | `cublasGemmStridedBatchedEx` | 512,512,128,32 | R_16BF/R_16BF → **R_32F** | COMPUTE_32F | DEFAULT_TENSOR_OP |
| score | `cublasGemmBatchedEx` | 512,512,576,32 | R_32F/R_32F → R_32F | COMPUTE_32F | DEFAULT_TENSOR_OP |
| context | `cublasGemmBatchedEx` | 512,512,512,32 | R_32F/R_32F → R_32F | COMPUTE_32F | DEFAULT_TENSOR_OP |
| `wv_b` | `cublasGemmStridedBatchedEx` | 128,512,512,32 | R_16BF/R_16BF → **R_32F** | COMPUTE_32F | DEFAULT_TENSOR_OP |

`cublasSetMathMode(CUBLAS_TF32_TENSOR_OP_MATH)` observed at handle setup and
`MathMode=CUBLAS_TF32_TENSOR_OP_MATH` reported in every call context (1,740
mentions). Every field matches the static table; the one refinement is the
Ex-variant split (absorb/`wv_b` strided-batched, score/context
pointer-batched — identical type semantics). `Ctype = R_32F` on all four
directly observes `prefer_f32_output`, closing the loop on the earlier
lattice falsification. Evidence: `runtime_dispatch_confirmation.json`
(40-entry probe `SHA256SUMS.txt`; raw logs on disk, gitignored).

---

# Addendum 2026-08-17 — Frozen 512-token end-to-end logit criterion (measurement-only)

Production-style run of the **unmodified frozen criterion**
(`abs(cpp−hf) ≤ 0.5 + 0.05·abs(hf)` over all 131,072 final-position logits +
top-1 agreement; comparator
`D:\llama.cpp-longcat-mtp\compare_longcat_sparse_gate3_logits.py`, SHA
`6976fbc0…`, reused verbatim) against the current branch arithmetic.

**Preflight all-PASS:** 19-variable environment-cleanliness sweep
(source-derived: the complete `LONGCAT_*` set of three, twelve `GGML_CUDA_*`
overrides, four cuBLAS logging vars — all absent); oracle `8825d92d…` and
prompt `d3c44b15…` verified; fresh result dir; binary provenance recorded
(HEAD `b009d6f68`, `llama-debug.exe` `df2a57f6…`, `llama.dll` `93466c40…`,
`ggml-cuda.dll` `502e50e8…`); placement `(29, 15, ATTN)` / 29/30 / 88936.14
MiB; live cuBLAS 6.14.11.1330; produced `-tokens.bin` = `4893d787…`; logits
524,288 B. `--save-logits` mode: eval callback disabled, no `LONGCAT_*`
diagnostics gated on, block-0 RoPE = production `ggml_rope_ext` angles.

**Baseline identity correction (gate 7):** the memorandum's "best early
finite common-path baseline (FA-off + F32 KV)" is
`sparse_512_fa_off_f32/…-00008.bin` (`1a8e37e2…`) — **not** the
`sparse_512_fa_off` bf16-cache variant (`f39f77b6…`, which scores 12,609
violations). The f32 baseline reproduces the memo's numbers digit-for-digit
(max_abs 1.84657693, mean 0.241643069, RMSE 0.306931687, cosine 0.999293288,
violations 2,122, top1 483, top20 20/20), validating the comparator chain
end-to-end before the new verdict was read. The current run's
`-ctk f32 -ctv f32` matches this baseline's configuration.

## Result — FAIL under the frozen criterion, with large movement

| Frozen metric | Clean pre-diagnostic baseline (`1a8e37e2…`) | **Current block-0 stack (`e14d95bf…`)** |
|---|---|---|
| violations | 2,122 / 131,072 | **40 / 131,072** (÷53; 98.1% removed) |
| worst_tolerance_ratio | 2.78345282 @ 15626 | **1.33129834 @ 100151** |
| max_abs_error | 1.84657693 @ 25433 | **0.851543427 @ 73210** |
| mean_abs_error | 0.241643069 | **0.130625596** |
| RMSE | 0.306931687 | **0.164743901** |
| cosine | 0.999293288 | **0.999799076** |
| top-1 | 483 = 483 ✓ | 483 = 483 ✓ |
| top-20 / top-100 | 20/20 · 92/100 | 20/20 · **96/100** |

**Interpretation, per the pre-fixed rules.** The criterion **FAILS** (40
violations > 0; top-1 agrees). The delta is attributed to the **aggregate
current block-0 diagnostic/corrective stack** (N-gram BF16/restore-F32,
block-0 RMSNorm semantics, Q-side BF16 chain, and Experiments A+B) relative
to the clean pre-diagnostic baseline — **not** to A+B specifically; the
A+B-specific causal results stand only on the dedicated block-0 captures.
This criterion traverses the entire model: layers 1–29 and the MLP/MoE paths
are not equivalently proven, so **no block-0 conclusion is drawn from the
final logits**, and the choice between production-RoPE completion, an
attention-core parity redesign, or a new downstream localization is **not
made from this run**. The remaining 40 violations (worst 1.33× its
tolerance) may lie in later attention layers, MLP/MoE, or other downstream
paths. **Stopped for review; no arithmetic was changed.**

Production-RoPE note: this run exercised ggml angle generation everywhere;
existing evidence (R0 control) bounds the block-0 RoPE share as small.

Artifacts: `cpp_logits_512_postAB/` (6-entry `SHA256SUMS.txt`,
`gate3_512_postAB.json`, `gate3_512_baseline_recheck.json`; logits/token
bins + run logs on disk, gitignored).

---

# Addendum 2026-08-17 — Downstream boundary walk (zero-new-run, measurement-only): no single divergence boundary; smooth growth, origin unresolved (inherited vs local)

> **Wording correction 2026-08-17 (review):** an earlier version of this
> addendum was titled "…the residual gap is distributed, not localized" and
> contained interpretation language treating the orthogonality/growth result
> as evidence of locally generated error throughout the trunk. That
> over-claimed. The walk is observational: smooth growth plus major direction
> change is consistent **both** with nonlinear propagation/rotation of the
> inherited block-0 error **and** with new local per-block discrepancies, and
> cannot distinguish them. The corrected reading below stands; the causal
> reset experiment (approved follow-up A) is the discriminator.

Executed per the approved fresh-session plan. 18 final-row `[3072]` C++/HF
boundary pairs, **zero new runs, no GPU, no arithmetic changes**. C++ side:
`cpp_attn0_mla_attnpath_512/` — production-angle A+B state, byte-identical to
`cpp_attn0_mla_expB_512/` on every surface used (committed Class-2 gate), and
representative of the frozen-512 measurement binaries because
`git diff 5b408206c..b009d6f68` touches only env-gated R0/R1 + callback
plumbing (proven inert by the Class-2 18/18 and probe 39/39 gates). HF side:
`pre-gate4\hf_logical0_stages_512_v4\` + `pre-gate4\hf_hidden_512_v4\`
(frozen runtime `a3bc3161…` re-hashed from the checkpoint dir, tokens
`4893d787…`, TF32 off, bf16 CUDA, `use_cache=False`).

Script: `analyze_longcat_downstream_boundary_walk.py`
(SHA `6c600599f01c7fc349478f9203c8ccd0ad28a2c510973bc9e666800810c633be`
after the review wording correction — numeric output verified identical
section-by-section to the pre-correction JSON; `py_compile` clean). Fail-closed: all 38 inputs SHA-gated against the
committed capture manifest / sidecar values; identity gates PASS (origin
`inp_embd_ngram` byte-exact C++≡HF `d0e9edc8…`; `stages\input` ≡
`hidden\inp_embd_ngram`; `stages\logical0_out` ≡ `hidden\logical_00`).

## Instrumentation validity — both known-answer anchors reproduce

- `attn0_resid` rel-RMSE **0.003901083** reproduces the committed
  **0.00390108** (Exp-B addendum).
- Endpoint reproduces the frozen-512 record digit-for-digit: **40**
  violations, RMSE **0.164743901**, cosine **0.999799076**, max_abs
  **0.851543427**, top-1 483/483.

## Semantic execution order — proven from source, recorded in the JSON

- HF (`modeling_longcat_flash_sparse.py` = `a3bc3161…`): `num_layers=14`,
  `num_hidden_layers=28` (config); trunk appends hidden states **before**
  each of the 14 logical layers plus once **after** `self.norm`
  (lines 1472-1489), so `logical_NN` = output of logical layer N (N=0..12)
  and `result_norm` = norm(output of logical layer **13**). The stages
  capture snaps `input → attn0_resid → mlp0_resid → attn1_resid →
  logical0_out` inside layer 0's forward (lines 207-286), ScMoE shortcut
  computed after attn0's norm but added only at `logical0_out`.
- C++ (`common/debug.cpp` 240-286, `src/models/longcat-flash-ngram.cpp`
  594-1117): `ffn_inp-0` → attn0_resid, `l_out-0` → mlp0_resid (even block:
  shortcut computed, **not** added), `ffn_inp-1` → attn1_resid,
  `l_out-(2N+1)` → `logical_NN` (odd block adds dense MLP[1] **+ paired
  shortcut**), `result_norm` = `output_norm` on the last main block output.
  One logical layer = two physical blocks. The nextn/MTP block is **outside
  the trunk on both sides** (HF: separate `LongcatFlashSparseMTP` module;
  C++: separate `graph_mtp` at index `n_layer`, `n_layer_nextn == 1`
  asserted).
- **Documented gap:** the `logical_12 → result_norm` step spans the entire
  **undumped logical layer 13** (physical blocks 26-27) plus the final
  RMSNorm, on both sides. No existing capture holds a boundary inside it.

## Result — trajectory (final row; full metrics in the JSON)

| boundary | rel-RMSE | err L2 | step ratio | cos(e,e_prev) | orth frac | cos(e, e_final) |
|---|---|---|---|---|---|---|
| `inp_embd_ngram` | 0 | 0 | — | — | — | — |
| `attn0_resid` | 3.901e-03 | 6.734e-03 | — | — | — | +0.011 |
| `mlp0_resid` | 4.718e-03 | 1.501e-02 | 2.229 | 0.440 | 0.898 | +0.023 |
| `attn1_resid` | 4.928e-03 | 1.763e-02 | 1.174 | 0.773 | 0.635 | +0.025 |
| `logical_00` | 5.719e-03 | 3.402e-02 | 1.930 | 0.484 | 0.875 | +0.049 |
| `logical_01` | 8.929e-03 | 8.966e-02 | **2.635** | 0.219 | **0.976** | +0.046 |
| `logical_02` | 9.878e-03 | 1.592e-01 | 1.775 | 0.466 | 0.885 | +0.093 |
| `logical_03` | 1.657e-02 | 3.430e-01 | 2.155 | 0.361 | 0.933 | +0.137 |
| `logical_04` | 2.287e-02 | 5.707e-01 | 1.664 | 0.676 | 0.737 | +0.200 |
| `logical_05` | 2.417e-02 | 7.551e-01 | 1.323 | 0.862 | 0.506 | +0.232 |
| `logical_06` | 2.581e-02 | 9.436e-01 | 1.250 | 0.875 | 0.484 | +0.279 |
| `logical_07` | 2.629e-02 | 1.206e+00 | 1.278 | 0.866 | 0.501 | +0.366 |
| `logical_08` | 3.368e-02 | 1.785e+00 | 1.481 | 0.766 | 0.642 | +0.475 |
| `logical_09` | 3.524e-02 | 2.255e+00 | 1.263 | 0.837 | 0.546 | +0.576 |
| `logical_10` | 3.402e-02 | 2.650e+00 | 1.175 | 0.911 | 0.413 | +0.624 |
| `logical_11` | 3.563e-02 | 3.571e+00 | 1.347 | 0.787 | 0.617 | +0.761 |
| `logical_12` | 3.485e-02 | 4.998e+00 | 1.400 | 0.761 | 0.649 | 1 |
| `result_norm` | 5.073e-02 | 5.559e+00 | (space change) | — | — | — |

Endpoint (descriptive): logits rel-RMSE 0.0203 vs `result_norm` rel-RMSE
0.0507; **no lm_head isolation is claimed** — no offline head projection was
performed; lm_head attribution remains **unresolved**.

## Reading — per the pre-agreed phrasing rules

1. **The residual frozen-512 gap is not localized at any single downstream
   boundary at logical-block granularity.** Error L2 grows ×743 from
   `attn0_resid` to `logical_12` in 15 steps of ratio 1.17-2.64 — **no step
   exceeds 3× the median ratio**; the largest single step
   (`logical_00 → logical_01`, ×2.64, orth 0.976) is a character change,
   not a discontinuity.
2. **Divergence grows or changes character at essentially every logical
   block**: the orthogonal (new-direction) error fraction exceeds 0.5 at
   13 of 15 steps. This observation is consistent **both** with distributed
   per-block error contributions compounding with depth (the proven block-0
   mechanism class — unrounded F32 GEMM outputs, TF32 on off-lattice
   operands, BF16 conversion boundaries, un-modeled MLP/MoE precision
   semantics — recurring in every block) **and, equally, with nonlinear
   propagation/rotation of the inherited block-0 error alone**: inherited
   input error passing through nonlinear MLP/MoE (including router top-k
   flips) can create new components, and a block Jacobian can rotate
   inherited error. **The walk cannot distinguish these hypotheses and no
   attribution of any step's growth to the local operation is made.**
3. **Seed direction persistence (not a causal bound):** the final trunk
   error is essentially orthogonal to the block-0 attention seed —
   cos(e_attn0_resid, e_logical_12) = **+0.011**; even `logical_00`'s full
   error reaches only +0.049. This bounds **direction persistence only**: a
   nonlinearly rotated/transformed inherited error would not project onto
   the seed direction, so this number does **not** bound the seed's causal
   contribution to the final error, and no conclusion about where the
   final error was *generated* follows from it. Discriminating inherited
   propagation from local generation requires a causal reset experiment.
4. The early trunk (through `logical_04`) carries the largest per-step
   growth ratios (×1.66-2.64); from `logical_05` onward growth settles to
   ×1.18-1.48 per block while rel-RMSE plateaus near 0.035 (state RMS grows
   with depth), then rises to 0.0507 across the `logical_13`+norm gap.

## Follow-up options (decision 2026-08-17: **A approved with review corrections; B declined**)

- **(A) Causal split via full-sequence oracle injection** (R0-pattern, new
  runs required): capture the HF **full-sequence** `[512, 3072]` hidden
  state at one boundary (e.g. `logical_00` / `l_out-1`; existing HF dumps
  are final-row only, and later layers attend over all positions, so a
  causally clean injection needs all 512 rows), then an env-gated C++
  callback overwrite at that node followed by the frozen-criterion endpoint
  measurement. Splits the 40 violations into
  upstream-vs-downstream-of-the-boundary **causally**, reusing the proven
  R0 injection pattern. Requires: one HF capture run + one C++ run + small
  callback-only plumbing, all measurement-only, all existing gates as
  regressions.
- **(B) Reviewed arithmetic plan** for per-block precision semantics
  (extend the proven block-0 KV/Q BF16-boundary pattern to all blocks
  and/or model MLP/MoE boundary semantics), gated on the frozen criterion.
  This is arithmetic work and remains **forbidden until its plan is
  reviewed and approved**.

Artifacts: `downstream_walk_512/downstream_boundary_walk.json`
(SHA `2e4854c794064f1eaf888c3d237bbff6526ca7a3bf01ecf69522df1b7297c5d0`
after the wording correction; numeric sections byte-identical to the
pre-correction version, with the proven semantic-order evidence embedded)
+ `SHA256SUMS.txt`; script as above. **Stopped for review; no arithmetic
was changed.**

---

# Addendum 2026-08-17 — Causal reset at logical_00: the downstream trunk locally regenerates the dominant share of the residual divergence

Approved follow-up A executed end-to-end (measurement-only; no arithmetic
changed anywhere; production RoPE; every C++ run under the pinned,
live-verified cuBLAS 6.14.11.1330 with `graphs reused = 0`).

## Experiment integrity chain (all gates PASS)

1. **Causal-cut proof** (source-referenced, embedded in
   `resid_inject_walk_512/resid_inject_walk.json`): on both sides the
   residual stream is the only mutable inter-layer carrier — C++ loop-carried
   state is exactly `inpL` + the layer-local `moe_shortcut`
   (consumed+nulled before `l_out-1`); the LSA indexer weights are loaded but
   absent from the trunk graph; HF at 512 tokens runs the dense
   `full-owner`/`full-reuse` fast path (512 ≤ index_topk 2048) with
   `topk_indices=None`, layer-local shortcut, `past_key_values=None`, and
   telemetry-only `last_lsa_*` writes; MTP outside the trunk on both sides.
   **Verdict: complete causal cut at `l_out-1` — no mutable bypass.**
2. **Instrumentation** (commit `2f827a91e`, `common/debug.cpp` only,
   env-gated): `LONGCAT_RESID_WALK_DUMP_DIR` whole-sequence `[512×3072]`
   dumps — `l_out-(2N+1)` → `logical_NN_full.bin` for N=0..13 (N=13 adds the
   previously undumped logical layer 13 = `l_out-27`) and `h_nextn` →
   `result_norm_full.bin` (post-final-norm before the row filter);
   `LONGCAT_RESID_INJECT_DIR` R0-pattern reset of `l_out-1` from
   `logical_00_oracle.bin`, fail-closed exit 87, injected before the host
   copy so the dump is the landing witness. Sweep list grows to 23 vars.
3. **Rebuild provenance** (instrumentation HEAD `2f827a91e`, no
   reconfiguration): `llama-common.dll` `aa646eb5…` is the only changed
   binary (it contains `common/debug.cpp`); `llama-debug.exe` `df2a57f6…`,
   `llama.dll` `93466c40…`, `ggml-cuda.dll` `502e50e8…` **byte-identical to
   the checkpoint set**. Both runs re-hashed the identical 4-binary set at
   preflight.
4. **HF full-sequence capture** (`capture_longcat_hf_resid_walk_512.py`
   `cd33d421…`, commit `2fce13c6a`; frozen runtime `a3bc3161…` + token
   stream `4893d787…` SHA-gated; TF32 off): row 511 of every prior surface
   byte-equals the committed `hf_hidden_512_v4` final-row oracle (14/14
   gates), and the **same-pass** final-norm hooks prove norm input ≡
   `layers[13]` output and norm output ≡ `hidden_states[14]` bytewise.
   Reset oracle `logical_00_oracle.bin` = `d810f93c…`; fresh `logical_13`
   row-511 = `0739efe6…`. Bins in `D:\lc_resid_walk_512\`; sidecars mirrored
   to `_external_artifacts/hf_resid_walk_512_20260817/`.
5. **Control run** (`cpp_resid_walk_control_512/`, harness
   `run_longcat_resid_walk_512.ps1`): placement `(29, 15, ATTN)`,
   offloaded 29/30, live cuBLAS pinned-path 6.14.11.1330, **29/29 final-row
   dumps byte-identical to the committed attnpath manifest** — the
   instrumented binary is proven inert on every pre-existing surface — and
   the 15 full-seq dumps present; provides `e_observed` at whole-tensor
   resolution.
6. **Injection run** (`cpp_resid_walk_inject_512/`): landing byte-exact
   (`logical_00_full.bin` == oracle; final-row dump == the HF `5292e88a…`
   oracle), upstream inertness 15/15 vs control, injection log line present.
   Consumption established per the corrected gate (static dataflow + runtime
   ordering); downstream movement observed at every boundary (a posteriori).
7. **Known-answer**: the row-511 slice of `e_observed` reproduces the
   committed observational-walk rel-RMSE at all 13 shared boundaries to
   ≤ 1e-12 (`analyze_longcat_resid_inject_walk.py` `fca3f2ca…`).

## Result — whole-tensor `[512, 3072]`, both error families

`e_observed = cpp_control − hf` (total: inherited + local),
`e_reset = cpp_inject − hf` (regenerated downstream of the byte-exact reset).

| boundary | obs rel-RMSE | reset rel-RMSE | **‖e_r‖/‖e_o‖** | cos(e_r, e_o) | reset step growth |
|---|---|---|---|---|---|
| `logical_01` | 1.334e-02 | 9.053e-03 | **0.679** | 0.259 | — |
| `logical_02` | 1.161e-02 | 9.709e-03 | 0.836 | 0.299 | 2.284 |
| `logical_03` | 1.534e-02 | 1.056e-02 | 0.688 | 0.389 | 1.426 |
| `logical_04` | 1.944e-02 | 1.233e-02 | 0.634 | 0.375 | 1.433 |
| `logical_05` | 2.703e-02 | 1.586e-02 | 0.587 | 0.322 | 1.537 |
| `logical_06` | 3.477e-02 | 2.106e-02 | 0.606 | 0.281 | 1.538 |
| `logical_07` | 5.004e-02 | 3.978e-02 | 0.795 | 0.458 | 2.178 |
| `logical_08` | 6.368e-02 | 5.278e-02 | 0.829 | 0.490 | 1.490 |
| `logical_09` | 6.683e-02 | 5.479e-02 | 0.820 | 0.458 | 1.125 |
| `logical_10` | 6.659e-02 | 5.297e-02 | 0.796 | 0.400 | 1.099 |
| `logical_11` | 6.824e-02 | 5.251e-02 | 0.769 | 0.373 | 1.152 |
| `logical_12` | 8.045e-02 | 6.870e-02 | 0.854 | 0.455 | 1.504 |
| `logical_13` | 9.309e-02 | 7.357e-02 | 0.790 | 0.446 | 1.051 |
| `result_norm` | 9.361e-02 | 7.235e-02 | **0.773** | 0.438 | (space change) |

Reset divergence is dense and immediate: at `logical_01`, 512/512 token rows
divergent from token 0, raw-equal 5/1,572,864.

## Dual stop rule — outcome

**First-raw = `logical_01`; first crossings at 0.01, 0.10, and 0.50 are all
`logical_01`. Raw onset and consequential growth coincide** — the
review-stop branch (separated onsets) is not triggered.

## Causal reading

1. **One logical layer regenerates most of its local gap under this
   intervention.** With byte-exact input, logical block 1 alone produces
   rel-RMSE 9.05e-3 — ‖e_reset‖ is **0.679 of the observed error L2 at that
   boundary** — before any inherited error exists downstream of the reset.
2. **Downstream-local regeneration is substantial and dominant under this
   intervention — but the ratios are not additive decompositions.** With
   everything upstream of `logical_00` made byte-exact, the endpoint error
   L2 that remains is 0.773 of the observed value (`result_norm`; 180.9 vs
   234.1). This proves, causally, that the downstream trunk **by itself**
   regenerates the dominant share of the endpoint divergence, resolving the
   observational indistinguishability of the previous addendum. **It does
   NOT establish an additive 77.3% downstream / 22.7% upstream partition of
   the error: the downstream network is nonlinear, so upstream and
   downstream contributions do not superpose** — the same nonlinearity that
   makes inherited error unrecoverable from observation alone also means
   these intervention ratios quantify one specific counterfactual (exact
   upstream), not a decomposition of the observed error into parts.
3. The regenerated error is only weakly aligned with the observed error
   (cos 0.26–0.49) and compounds smoothly (×1.05–2.28/step with rising step
   direction-persistence) — the same character as the observational walk,
   now with a causal license.
4. **Consistency note (not a new attribution):** every element of the
   block-0 corrective stack is `il == 0`-gated, so physical blocks 1–27 run
   entirely uncorrected arithmetic (uncorrected block-0 attention alone
   measured rel 0.0152 at its residual before corrections). A per-layer
   local generation rate of ~9e-3 (two attentions + two dense MLPs + ScMoE)
   is consistent with that uncorrected mechanism class recurring in every
   block. Which sub-operation dominates inside a clean-input block is
   exactly what the proposed next measurement determines.
5. Caveat: `e_reset` at boundary N>1 includes error inherited from
   post-reset boundaries before N; only `logical_01` is purely
   single-layer-generated. `result_norm` is a post-norm space change.

## Proposal per the stop rule (design only, NOT begun)

Raw onset and consequential growth coincide at `logical_01` → **sub-boundary
instrumentation inside logical block 1 only** (physical blocks 2–3):

- C++: extend the `LONGCAT_RESID_WALK_DUMP_DIR` spec set with full-sequence
  `ffn_inp-2` (block-1 attn0 residual), `l_out-2` (block-1 mlp0 residual),
  `ffn_inp-3` (block-1 attn1 residual) — callback-only, same writer.
- HF: a layer-1 stages capture reusing the proven monkey-patch pattern of
  `capture_longcat_sparse_hf_512_logical0_stages.py` (snap `attn0_resid`,
  `mlp0_resid`, `attn1_resid` of `trunk.layers[1]`, full-sequence), gated on
  the same frozen runtime/token stream, with row-identity gates against the
  existing captures where applicable.
- One control-style comparison of the injected run's block-1 sub-boundaries
  against the HF layer-1 stages (the injection run already provides
  clean-input conditions) localizes where inside one clean-input logical
  layer the 9.05e-3 is born (attn[0] vs dense MLP[0] vs attn[1] vs
  dense MLP[1]+ScMoE join).

Requires one instrumentation commit + one HF capture + one C++ injection
re-run with the extended dump set, all measurement-only. **Stopped for
review; no arithmetic was changed.**

Artifacts: `resid_inject_walk_512/resid_inject_walk.json`
(SHA `a4a24cf34035d21fa6551f74c5187e422f789f2203583a1ee6d9761cddbf64c3`
after the non-additivity wording correction — numeric sections verified
identical to the pre-correction version; causal-cut proof + run provenance
embedded) + `SHA256SUMS.txt`;
run manifests + provenance sidecars committed in
`cpp_resid_walk_control_512/` and `cpp_resid_walk_inject_512/` (bins/logs on
disk, gitignored); harness `run_longcat_resid_walk_512.ps1`; comparator
`analyze_longcat_resid_inject_walk.py` = `f5429368…` after the wording
correction (`py_compile` clean).
