# LongCat Sparse Parity — Status 2026-08-17

First authoritative status from the Windows 11 / RTX PRO 6000 workstation.
Supersedes the *indicative* localization in `STATUS_2026-08-16.md`. All
guardrails in `CLAUDE.md`, `NEXT_ACTION.md` and
`HANDOFF_MEMORANDUM_2026-08-15.md` remain in force unchanged.

**Branch:** `claude/longcat-win11` (from `35ad2e63a`)
**Capture:** `cpp_attn0_mla_blackwell_512/` (+ `SHA256SUMS.txt`)

---

## Headline

The Blackwell C++ block-0 MLA capture is **done and gated**, the comparison
against the authoritative HF oracles is **done**, and the first genuine
divergence is not where the Ampere run indicated.

| | Ampere (indicative) | **Blackwell (authoritative)** |
|---|---|---|
| First divergent surface | `kv_a_proj_with_mqa` | `kv_a_proj_with_mqa` |
| First **genuine** divergence | `o_proj` | **`kv_a_layernorm`** |
| Q path | "essentially exact" | **byte-exact, all 3 surfaces** |
| `kv_a_layernorm` | below noise floor, unconcludable | **real, and fully explained** |

**No LongCat arithmetic was changed.** Everything below is measurement.

---

## The capture is authoritative

All three frozen gates reproduce **byte-exact**:

| Artifact | SHA256 | |
|---|---|---|
| `logical0_attn0_resid.bin` | `2c804a35a0397e380d77f08d2a7ffd11fc6e4672722c31a98f7f96620c2a4a4e` | **PASS — the arbiter** |
| `inp_embd_ngram.bin` | `d0e9edc8503e388d62b27f39c6ad24560021b1f4533410bf4f86a28fef4ea45f` | PASS |
| `logical0_attn0_norm.bin` | `a1c4c20cf346cbc5d13a0ea3d1d142f1b54f8e024ff3f04fdb31e9120e75b0af` | PASS |

Placement reproduced the frozen run exactly: `(n_layer, n_part, overflow_type)
= (29, 15, ATTN)`, `id_dense_start=0`, `offloaded 29/30`, `CUDA0 model buffer
88936.14 MiB`, `CPU KV 10.12 MiB` + `CUDA0 KV 283.50 MiB`, compute
`1654.00`/`24.03 MiB`. Tokenization: 512 pieces, all ID 483.

---

## Result

`compare_longcat_attn0_mla_stages.py`, full sequence, 512 tokens:

| Surface | max_abs | rel-RMSE | cosine | exact% | first divergent token |
|---|---|---|---|---|---|
| `q_a_proj` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `q_a_layernorm` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `q_b_proj` | 0 | **0** | 1.0000000000 | **100.00%** | — |
| `kv_a_proj_with_mqa` | 0.0254898 | 0.00122158 | 0.9999993376 | 0.00% | 0 |
| `kv_a_layernorm` | 0.0690088 | 0.00162006 | 0.9999999140 | 0.00% | 0 |
| `o_proj` | 0.0279193 | **0.0194126** | 0.9998376748 | 0.00% | 0 |

`analyze_longcat_attn0_mla_bf16_boundary.py` separates precision boundaries
from real differences. Every HF surface is confirmed 100% on the BF16 lattice,
so "already BF16" is verified, not assumed.

| Surface | exact% raw → BF16-rounded | rel-RMSE raw → rounded | verdict |
|---|---|---|---|
| `q_a_proj` / `q_a_layernorm` / `q_b_proj` | 100% → 100% | 0 → 0 | **byte-exact** |
| `kv_a_proj_with_mqa` | 0.00% → **100.00%** | 0.00122 → **0** | **pure BF16 boundary** |
| `kv_a_layernorm` | 0.00% → 45.22% | 0.00162 → 0.000916 | **real** |
| `o_proj` | 0.00% → 12.58% | 0.01941 → 0.01948 | **real** |

### The whole Q path is byte-exact

`q_a_proj`, `q_a_layernorm` and `q_b_proj` are identical to the HF Blackwell
oracles across all 512 tokens — rel-RMSE exactly 0, cosine exactly 1, 100% of
elements. On Ampere these read 2.0e-06 / 0 / 6.2e-06; that residue was the
cross-GPU noise floor, not the model. The Q-BF16 diagnostic is doing its job
exactly, and the memorandum's "Q-BF16 made things worse" is now definitively
reframed: it made Q *correct*.

### `kv_a_proj_with_mqa` is a pure output boundary

Rounding the C++ F32 output to BF16 makes it **exactly** equal to HF — 100.00%
of 512x576 elements, rel-RMSE 0. Not 99.31% as Ampere indicated; **perfect**.
C++ computes the GEMM in F32 where HF stores BF16; nothing else differs.

### `kv_a_layernorm` is the first genuine divergence — and it is explained

This is the reading that sat below the Ampere noise floor and genuinely
required this run. `analyze_longcat_attn0_kv_a_norm_semantics.py` reconstructs
the norm offline from the GGUF weight the C++ run actually loaded:

1. **Sanity** — the offline F32 model reproduces the **C++** surface at
   rel-RMSE 0, 100.00% exact. The reconstruction and weight are correct.
2. **Control** — `bf16(C++ kv_a_proj[:, :512]) == HF kv_a_proj[:, :512]` is
   `True`. The upstream input differs *only* by the rounding boundary above.
3. **Sweep** — feeding HF's own byte-identical BF16 input through llama.cpp's
   F32 norm still does **not** reproduce HF (rel-RMSE 0.00208, 0% exact). So
   the divergence is in the norm's precision semantics, not its input.

| Cast ordering (eps=1e-6) | rel-RMSE | exact% |
|---|---|---|
| A `f32(norm) * f32(w)` → f32 — **llama.cpp today** | 0.00207546 | 0.00% |
| B `bf16( f32(norm) * f32(w) )` | 0.000359351 | 68.59% |
| **C `bf16( bf16(norm) * f32(w) )`** | **0** | **100.00%** |
| **D `bf16( bf16(norm) * bf16(w) )`** — HF RMSNorm pattern | **0** | **100.00%** |
| E `bf16(norm) * bf16(w)` → f32 | 0.0010619 | 2.54% |

**HF rounds the normalized activation to BF16 *before* the weight multiply,
and rounds the product to BF16. llama.cpp keeps both in F32.** That single
ordering difference accounts for the surface *exactly* — max_abs 0.

C and D coincide because the norm weight is already 100% on the BF16 lattice.
eps 1e-6 and 1e-8 both reproduce exactly; **1e-5 is excluded** (rel-RMSE
9.5e-07), independently confirming the 1e-6 the C++ already uses.

### `o_proj` — real, but its input is already wrong

rel-RMSE 0.0194, ~12x `kv_a_layernorm`, and BF16 rounding does not touch it
(0.01941 → 0.01948). It is a genuine divergence. **But no independent defect is
claimed**: `o_proj` consumes K/V derived from `kv_a_layernorm`, which is itself
divergent, so its error is at least partly inherited. Whether anything remains
wrong *after* the KV boundaries are corrected cannot be determined from this
capture and must be re-measured, not assumed.

This corrects the Ampere reading, which named `o_proj` the first genuine
divergence only because `kv_a_layernorm` sat below its noise floor.

---

## Environment finding — cuBLAS version silently breaks every gate

The first gated run **failed**: `logical0_attn0_resid` came out
`49d729e16b09b7d113e1d774851364b4c31ee1ff98608505cbe52122824fc928` while both
upstream anchors passed and placement was bit-identical to the frozen run.

Running the **historical** `llama-debug.exe` — the exact binary that produced
`2c804a35…` on 2026-08-15 — reproduced the same `49d729e1…`. So the rebuild was
never at fault; the frozen residual had stopped being reproducible on this
machine.

Cause: `ggml-cuda.dll` imports **`cublas64_13.dll` by bare name**, and this
machine's `PATH` lists `CUDA\v13.0\bin\x64` **before** `v13.2`. The process was
loading cuBLAS **6.14.11.1300** (v13.0) instead of the **6.14.11.1330** (v13.2)
the build targets. Prepending the v13.2 directory restored `2c804a35…`
byte-exact.

The failure signature is diagnostic in itself: custom elementwise kernels
(`inp_embd_ngram`, `attn_norm-0`) stayed byte-exact while the GEMM-dependent
residual moved — exactly what a cuBLAS kernel-selection change produces.

> **Every future run on this workstation must pin cuBLAS to v13.2**, e.g. by
> prepending `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v13.2\bin\x64`
> to `PATH` for the child process. Without it the frozen gates fail silently
> and any comparison is invalid.

---

## Corrections to `WIN11_CPP_MLA_CAPTURE_RUNBOOK.md`

Its section 3 invocation does not reproduce the baseline. Evidence from the
frozen log `cpp_attn0_hf_rmsnorm_mlaeps1e6_qbf16_512.log` and the source:

| Runbook says | Reality |
|---|---|
| `-ngl 99` | **not passed.** `common/fit.cpp:378` throws `"n_gpu_layers already set by user … abort"`, yet the frozen logs all end `successfully fit params to free device memory` |
| `-ot ffn_*=CUDA_Host` | **not passed.** No override line appears anywhere; the MoE split came from auto-fit's `n_part` mechanism |
| "offloaded 30/30 layers" | **29/30.** The 30/30 is the fit's *initial probe*; every final load is 29/30 |
| (not mentioned) | **`-fitt 4096` required.** Default is 1024 MiB (`common/common.h:473`); the frozen log records `free memory target of 4096 MiB` |
| (not mentioned) | **`-b 4608` required** to match `n_batch` (verified numerically inert, but it aligns the log) |
| (not mentioned) | **`-v` required.** `LOG_TRC` needs verbosity ≥ 4 and `LOG_DBG` ≥ 5; default is 3 (`common/log.cpp:29`), so without it the fit trace is not emitted at all |
| (not mentioned) | **cuBLAS must be pinned to v13.2** (above) |

Also worth recording: `common/log.cpp:89-93` sends `GGML_LOG_LEVEL_NONE` to
**stdout** and everything else to **stderr**. The token-piece output is `LOG(...)`
at level `NONE`, so the tokenization gate reads stdout while the placement gate
reads stderr. Redirecting the two streams to separate files splits the gates.

`-ctk f32 -ctv f32` is confirmed by arithmetic, not just by the runbook:
4608 x 576 x **4 B** = 10.125 MiB/layer, matching `CPU KV 10.12 MiB` (1 layer)
+ `CUDA0 KV 283.50 MiB` (28 layers) = 29 layers.

---

## Build provenance

Fresh build directory `D:\llama.cpp-longcat-claude-build-cuda132`, configured
with venv cmake 4.4.2, VS 17 2022 x64, CUDA v13.2 toolset, `GGML_CUDA=ON`,
`CMAKE_CUDA_ARCHITECTURES=120a-real`, `LLAMA_BUILD_TESTS=OFF`.

- **182/182** reproducibility-relevant `CMakeCache` entries identical to the
  historical build. The 17 differences are all generated or path-dependent and
  were recorded, not forced.
- Compile-relevant sources vs `pre-gate4`: `ggml/` (1272 files) and
  `examples/debug/` (3) byte-identical in full; across `src/` (209) and
  `common/` (71) exactly two files differ — the known callback-only instrumented
  pair. Every CUDA kernel is bit-for-bit the frozen source.
- The historical build passes an extra `-I …\v13.2\include\cccl`; investigated
  and proven **inert** — nvcc reaches the same directory through a built-in
  path, 1374/1377 headers resolve to the same file and the other 3 are
  byte-identical duplicates.
- `llama-debug.exe`, `llama.dll`, `ggml.dll`, `ggml-cpu.dll`, `ggml-base.dll`
  match historical sizes exactly. `llama-common.dll` +5,120 B (instrumented
  `debug.cpp`). `ggml-cuda.dll` +13,312 B — the frozen gate settles equivalence
  empirically, and the historical binary was shown to produce identical output
  to the rebuild.

---

## Next action

The first genuine divergence is identified **and** its mechanism is known, so
the standing block on changing arithmetic is discharged for this specific
boundary. The narrowest defensible next step:

1. Add the missing BF16 boundary at `kv_a_proj_with_mqa` (output → BF16).
2. Apply HF RMSNorm cast semantics at `kv_a_layernorm`:
   `bf16( bf16(x * rsqrt(var + eps)) * w )`.
3. Re-run this capture and re-measure. Both KV surfaces should become
   byte-exact; then re-measure `o_proj` to see what, if anything, of its
   0.0194 survives once its input is correct.

Still forbidden until that re-measurement lands: touching MLP/MoE, a production
FA fix, a repaired 2050 run, and any widening of the frozen logit criterion.

---

## Artifacts

| Path | Contents |
|---|---|
| `cpp_attn0_mla_blackwell_512/` | six MLA surfaces + sidecars, frozen anchors, `SHA256SUMS.txt` |
| `…/comparison_authoritative.json` | full-sequence HF↔C++ metrics |
| `…/bf16_boundary_authoritative.json` | boundary-vs-real classification |
| `…/kv_a_norm_semantics_authoritative.json` | cast-ordering sweep |
| `analyze_longcat_attn0_mla_bf16_boundary.py` | `67d6dae7dc78670441bf64d804d966b6cee6013513c8d29191018cd5bf59459e` |
| `analyze_longcat_attn0_kv_a_norm_semantics.py` | see `ARTIFACT_SHA256_20260817.txt` |

Both new scripts are `python -m py_compile` clean. `*.bin` and `*.log` remain
gitignored; the run logs stay on disk beside the capture.

---

# Addendum 2026-08-17 — Experiment A result (KV BF16 boundary + norm cast semantics)

Executed after independent adversarial review (Fable 5 Max) **CONFIRMED WITH
CORRECTIONS**: experiments staged as A (the two oracle-gated changes) then B
(post-scale round, ungated, separate); cuBLAS contract made durable in
`CLAUDE.md`/`NEXT_ACTION.md` first; `2c804a35…` retired as a gate and preserved
as the immutable old-arithmetic baseline. The review also corrected the epsilon
claim — eps=1e-6 is fixed **by source** (`LongcatFlashRMSNorm.__init__` default,
constructed without override); the sweep excludes 1e-5 by one element and
cannot distinguish 1e-6 from 1e-8 — and verified the projection boundary at
integer precision: 294,912/294,912 elements, both splits.

**Commit `3d16341dc`** — `il == 0` KV branch only, mirror of the accepted
Q-side pattern: full-576 BF16 roundtrip on `kv_cmpr_pe` before the split views
(D1), and `rms_norm(1e-6)` → BF16 → F32 → `ggml_mul(w)` → BF16 with the
callback on the rounded tensor (D2). No post-scale round.

**Run** `cpp_attn0_mla_expA_512/`: preflight verified placement
`(29, 15, ATTN), id_dense_start=0` **and the live-process cuBLAS module**
(`v13.2\bin\x64\cublas64_13.dll`, 6.14.11.1330). Tokenization 512 × `(483)`.

## Gates — all PASS

| Surface | Result |
|---|---|
| `inp_embd_ngram` / `logical0_attn0_norm` | `d0e9edc8…` / `a1c4c20c…` unchanged |
| Q trio | `ddf69fe4…` / `956bd3e8…` / `4f3b647b…` unchanged |
| **`kv_a_proj_with_mqa`** | **= HF oracle `513390…` byte-exact** |
| **`kv_a_layernorm`** | **= HF oracle `b44cc101…` byte-exact** |

The in-graph run reproduced the offline byte-exact predictions exactly.
**Five of the six MLA surfaces are now hash-identical to the HF oracles.**

## Measurements (not gates)

| Quantity | Old arithmetic | Experiment A |
|---|---|---|
| `o_proj` rel-RMSE | 0.0194126 | **0.00606403** (÷3.2) |
| `o_proj` after BF16 rounding | 0.0196 (12.54% exact) | 0.006276 (26.61% exact) — still REAL |
| attn0 residual vs HF `2e9b65eb…` | rel-RMSE 0.0152371 | **0.00568733** (÷2.7), max_abs 0.0032950 |
| residual hash | `2c804a35…` (retired baseline) | `ce1b7836…` (recorded) |

Reading: the majority of the historical `o_proj`/residual divergence was
inherited from the KV path, as hypothesized. The surviving 0.006 remainder is
real and lies in the downstream cluster: the post-scale BF16 round (HF
`modeling_longcat_flash.py:426` — **Experiment B**), then bf16 cos/sin RoPE
(`:124`, `:433`), KV-cache dtype semantics, softmax `.to(query.dtype)`
(`:271`), absorption order, and the `wo` GEMM.

Artifacts: `cpp_attn0_mla_expA_512/` with `SHA256SUMS.txt` (33 files),
`comparison_expA.json`, `bf16_boundary_expA.json`. Source
`src/models/longcat-flash-ngram.cpp` = `21d62d48…` after commit `3d16341dc`.

---

# Addendum 2026-08-17 — Experiment B result (post-scale BF16 round)

**Commit `5b408206c`** — the one remaining source-documented KV boundary,
separated from Experiment A per review because no capture surface gates it:
HF scales the BF16 `k_pass` tensor so the scaled result is stored in BF16
(`modeling_longcat_flash.py:426`); llama.cpp scaled in F32. For `il == 0`:
scale in F32 → round BF16 → widen F32, mirroring the accepted Q-side
post-scale rounding. Four non-comment lines.

**Run** `cpp_attn0_mla_expB_512/`: preflight verified placement and the
live-process cuBLAS module (v13.2, 6.14.11.1330), as in A.

## Gates — all Experiment A gates still PASS

Anchors, Q trio, and both KV surfaces unchanged and byte-exact to the HF
oracles (every gated surface is upstream of the scale). 7/7 PASS.

## Attribution — delta(A→B)

| Quantity | Old arithmetic | Exp A | **Exp B** |
|---|---|---|---|
| `o_proj` rel-RMSE (vs HF oracle) | 0.0194126 | 0.00606403 | **0.00475900** |
| `o_proj` exact after BF16 round | 12.54% | 26.61% | 33.25% — still REAL |
| attn0 residual rel-RMSE (vs HF `2e9b65eb…`) | 0.0152371 | 0.00568733 | **0.00390108** |
| residual max_abs | 0.0135617 | 0.0032950 | **0.0010450** |
| residual hash | `2c804a35…` | `ce1b7836…` | `7e05940b…` |

The post-scale round moves both downstream measurements toward HF by a further
~22-31% on top of A. Cumulative from the old arithmetic: residual **3.9×**
closer, `o_proj` **4.1×** closer. The surviving `o_proj` remainder
(rel-RMSE 0.00476; BF16 rounding does not explain it — 0.005004 after,
33.25% exact) is the genuine downstream divergence, now attributable to the
still-unmodeled cluster in HF source order: bf16 cos/sin RoPE (`:124`,
`:433`), bf16 attention path (softmax `.to(query.dtype)` `:271`, bf16
SDPA/cache vs C++ F32 cache), the absorption ordering (llama.cpp folds `wk_b`
into `q_nope` where HF does not), and the `wo` GEMM input dtype.

Artifacts: `cpp_attn0_mla_expB_512/` with `SHA256SUMS.txt` (33 files),
`comparison_expB.json`, `bf16_boundary_expB.json`. Source
`src/models/longcat-flash-ngram.cpp` = `42bd0db2…` after commit `5b408206c`.

## Standing state after A + B

Six of six pre-attention block-0 MLA surfaces (Q trio, both KV surfaces, plus
the `kv_cmpr_pe` full-576 projection) are byte-exact to the HF Blackwell
oracles. The first genuine divergence is now **inside the attention/output
path** (`o_proj`), exactly where the memorandum's interpretation table pointed
next. Still forbidden until that remainder is diagnosed: MLP/MoE work, a
production FA patch, a repaired 2050 run, and any widening of the frozen logit
criterion.
